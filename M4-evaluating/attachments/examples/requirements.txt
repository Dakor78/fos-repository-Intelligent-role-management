# Модуль IV — зависимости для примеров (Python 3.12)
# Версии зафиксированы под проверенное окружение.
# Установка:  pip install -r requirements.txt
#
# Корзина задач сохраняется в data/eval_suite.json при первом запуске примера 2.

# --- Языковая модель и eval-примеры ---
openai==2.46.0          # клиент OpenAI-совместимого API (агент, судья, генерация задач)
pydantic==2.13.4        # схема задачи корзины
pandas==3.0.3           # таблицы результатов и бенчмарков
numpy==2.2.6            # транзитивная зависимость

# --- Метрика согласованности (каппа Коэна) ---
scikit-learn==1.9.0
scipy==1.16.1

# --- Пример на фреймворке (LangChain / LangGraph) ---
langgraph==1.2.9
langchain==1.3.14
langchain-openai==1.3.5

# --- Запуск и исполнение ноутбуков ---
ipykernel==7.3.0
jupyter-client==8.9.1
nbformat==5.10.4
nbconvert==7.17.1

# --- Опционально: пример 06 (публичный бенчмарк τ²-bench) ---
# Ставится из GitHub (в PyPI одноимённого пакета нет); тянет litellm:
#   pip install "git+https://github.com/sierra-research/tau2-bench.git"
