# Модуль III — зависимости для примеров (Python 3.12)
# Версии зафиксированы под проверенное окружение.
# Установка:  pip install -r requirements.txt
#
# Датасеты не требуются — учебный домен реализован в памяти (mas_domain.py).

# --- Языковая модель и агентные примеры ---
openai==2.46.0          # клиент OpenAI-совместимого API (модель и вызов инструментов)
pydantic==2.13.4        # типизированные план и контракты межагентных сообщений
pandas==3.0.3           # таблицы бенчмарка (пример 05)
numpy==2.2.6            # транзитивная зависимость pandas
requests==2.32.5        # запрос списка моделей у провайдера (GET /v1/models), опционально

# --- Пример на фреймворке (LangChain / LangGraph) ---
langgraph==1.2.9
langchain==1.3.14
langchain-openai==1.3.5

# --- Запуск и исполнение ноутбуков ---
ipykernel==7.3.0
jupyter-client==8.9.1
nbformat==5.10.4
nbconvert==7.17.1
