# Модуль I — зависимости для примеров (Python 3.12)
# Версии зафиксированы под проверенное окружение.
# Установка:  pip install -r requirements.txt

# --- Языковая модель ---
openai==2.46.0          # клиент OpenAI-совместимого API (примеры 02-04)

# --- Тема 2: токенизация ---
tiktoken==0.13.0        # подсчёт токенов, сравнение кодировок (пример 01)

# --- Таблицы результатов ---
pandas==3.0.5
numpy==2.5.1            # транзитивная зависимость pandas

# --- Тема 3: граф исполнения на фреймворке (пример 05) ---
langgraph==1.2.9
langchain==1.3.14
langchain-openai==1.3.5

# --- Запуск и исполнение ноутбуков ---
ipykernel==7.3.0
jupyter-client==8.9.1
nbformat==5.10.4
nbconvert==7.17.1
